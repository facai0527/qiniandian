# 六个画面，而非六个网页

## 最新导航修正：居中短款悬浮导览

- 新增 `src/compact-navigation.css`：撤下横贯屏幕的大底板，桌面导航宽约642px、高66px，保留天坛印记、祈年殿、六章节与自由观察；取消章节小编号，当前章节用墨色胶囊强调。沿用原有色彩，不新增背景模糊负担。
- 761–900px收紧间距；手机导航高58px、最大宽380px，章节菜单展开为三列两行，入口保持44px触摸高度。
- `check-navigation.cjs` 已通过1869、1440、1024、768、760、390、320宽度：居中/无越界、无内容相交，跳转和当前章节正确，手机展开、选中收起、Escape与外部点击关闭正常，自由观察与品牌回首页可用，无页面脚本错误。已查看桌面及390/320窄屏实际截图。
- 修改仅限导航样式及其导入；已有模型、镜头、背景、构成标签和结尾内容保留。

## 最新界面修正：构成标签与结尾排版

用户明确“三重屋顶”指的是构成展开画面里的介绍标签，而不是屋顶模型、特写或镜头。此次仅修改HTML与新增 `src/editorial-details.css`（时间线脚本仅增加样式导入）：

- 五个构成标签改成序号、衬线名称与一行短说明，细引线连接对应位置；屋顶注签改侧向排列，避免遮住顶部菜单，内部注签移出屋顶轮廓。标签不是按钮。
- 平板缩减标签信息；手机改成短名称和左右错位引线，避免320窄屏柱梁/内部标签互相覆盖。
- 结尾页统一居中版心，桌面左侧大标题、右侧正文及重看/官网入口，下方通栏资料折叠与页脚；手机按阅读顺序纵向排版。原有结尾正文保留。
- 不修改模型、材质、光照、镜头、分组拆解、背景视频及章节滚动锚点。
- `check-editorial.cjs` 在1869、1440、1024、768、390、320宽度检查并截图：标签无相互重叠/横向越界，结尾内容不被固定导航遮挡，无水平溢出，资料可鼠标/键盘展开收起，重看入口准确回到原始相机与节点变换。最新截图为工作区 `editorial-<宽度>-labels/ending/sources.png`。

## 当前视觉版本：金箔山水画境

用户进一步提供金/灰米/墨色卡和金箔山水参考，明确要求画境而不是简单换色。现在使用实际生成的横屏与竖屏画作，包含金箔、宣纸肌理、远山、墨松、月亮及水面。保留模型、布局、运镜、文案与交互，无配色按钮。主模型仍是真实 GLB，不把画作当作可交互模型。

最新背景是用户提供的横/竖视频，按视口只加载一段，滚动与自由观察不暂停。已移除全部整图位移与合成云、水、碎片层。用户视频保留720p/24fps原视频流，仅移除音轨并前置索引；两个网页副本分别约7.77/8.15 MB。手动暂停、减少动态、隐藏/离屏暂停和失败重试保留；减少动态首次打开不请求视频，以原画静态显示。原视频首尾不严格匹配，循环交界可能跳变。无第二个 WebGPU 场景、无背景逐帧 JS。仅本地预览，未发布。

## 最新模型渲染

按北京市公园管理中心和北京市政府实景资料调整蓝瓦、朱柱、鎏金和汉白玉；分别处理琉璃釉面、木漆、金属和石材粗糙度。加入一次性预计算环境反射、暖侧光和真实方向光阴影，藻井保留补光。阴影仅在部件变化时更新且限频，最终静止状态精确刷新，不随视频逐帧重绘。来源、参数和限制见 `RENDERING-NOTES.md`。原有几何/嵌入图片/Blender和GLB原件均未改写。

## 本轮调整方向

已读取用户指定的 `https://github.com/dayinji/sen-3d-resume.git`（main: `c9a9fe373cde72c77ff7f2dabde17fb79dce89b3`），并实际查看其线上站点。参考其全屏固定三维背景、右侧滚动文案时间线、文字到视口约 30% 时的镜头停靠，以及停留—推进—停留的连续运镜。没有复制对方人物模型、肖像、个人文案或品牌素材。

开场居中展示整体，中间四幕为左侧裁切近景；按最新反馈，最后一幕先拉回完整全景，再展开现有建筑分组。桌面文字是真实文档流向上滚动，而不是在同一位置切换六张卡片。小屏使用模型在上、文案在下的独立构图，保留自然页面触摸滚动。

正文已改为介绍祈年殿的祈谷用途、屋顶形制、彩画、门窗、藻井及柱网寓意，移除 GLB、节点、材质、运镜和建模说明。史实核对天坛公园官网与北京市公园管理中心；页尾“延伸阅读与展示说明”提供原始资料链接与必要的解释性展示声明。

当前首页是使用已有 GLB 的连续滚动原型。`history.html` 保留改动前的历史导览页面，旧 `main.js` / `styles.css` 均保留。

## 本地启动

在此目录执行 `npm run dev -- --host 127.0.0.1 --port 5177 --strictPort`，打开 http://127.0.0.1:5177/qiniandian/ 。仅绑定回环地址，没有外网部署。

## 时间线

| 镜头序号 | 关键画面 | 同一场景中的变化 |
| --- | --- | --- |
| 01 | 总览 | 开场整体建立镜头，居中主体与下方标题 |
| 02 | 琉璃 | 靠近屋瓦和檐口，左侧大幅裁切 |
| 03 | 檐下 | 靠近局部梁枋斗栱，Hall_Roof 整组轻抬 0.16 个归一化场景单位 |
| 04 | 门窗 | 屋顶复原；一段门窗与柱间关系的特写 |
| 05 | 藻井 | 外围在相机进入前淡出，仰视现有藻井中心与锦纹 |
| 06 | 建筑构成 | 先恢复完整全景，停留后展开屋顶、柱梁、门窗和内部；台基固定，宝顶匾额随屋顶移动 |

时间线由实际文案锚点计算，不硬编码为六段等高百分比。常规段落前后各 24% 用于阅读停留，中间 52% 平滑运镜。最后一段加长：局部进度 6%–49% 拉远恢复全景，49%–63% 保持完整，63%–93% 逐渐展开，之后停留。全程使用时间阻尼抑制快速滚动跳变。审阅图中的序号不是页面滚动条的百分比。

展开仅修改已导出的六个顶层分组的位置；保留原旋转、缩放和父子关系。屋顶的三层瓦面仍属于一个节点，未假装逐层独立拆解。每次从初始变换加绝对位移，反向返回完整全景时可准确复原。建筑名称标签只是辅助标注，不是第二套模型。

连续插值相机角度、距离、目标与分组变换；每次计算均从保存的初始变换重建，不叠加位移。默认滚轮与触摸用于页面滚动，只有点击“自由观察”才接受模型拖拽。缩放有独立按钮，滚动、Esc 或复位按钮退出自由模式。

## 现有纹理绑定修正

检查发现 GLB 合并后的部分绘彩网格在 UV0 中只有填充值，而真实展开仍保留在其他 UV 层。v02 网页衍生版先重新绑定现有 UV1（内部梁枋）、UV2（龙井柱）、UV3（藻井中心），并用 TEXCOORD_4 补回天花中 UV0 填充的顶点，再裁去无用 UV 顶点数据。沿用原有八张嵌入图片，不生成新贴图，不改变原始 Blender/v01 GLB 文件，不把材质子网格伪装成独立动画零件。绘彩按非金属材质着色，内部补光帮助阅读向下的天花面。

## 滚动性能修复（2026-09-24）

- 原桌面模型约 359 万面，双面透明过渡时最高提交约 712 万面。默认改用 v02 桌面约 145 万面 / 12.62 MB、移动约 115 万面 / 11.52 MB 的衍生资源。
- 瓦片按自身连通部分的尺寸控制简化误差，避免以整座建筑尺寸减面导致细小瓦片消失。六组名称、父子关系和初始变换均保留；藻井与彩绘使用更保守的误差限制。
- 双面透明过渡改为单次绘制，提前准备透明和不透明着色器；移除覆盖动态三维的大面积实时背景模糊，以深色渐变保持文字可读性。
- 滚动事件仅更新目标进度；视口尺寸缓存，文字更新集中在同一动画帧。阅读停留区不重复绘制相同姿态，静止与后台时停止渲染循环，自由观察静止时也不重绘。
- 根据连续慢帧降低三维画布分辨率（最低像素比 0.85）；文字始终保持屏幕原生清晰度。初始上限桌面 1.35、手机 1.15，不在滚动中来回切换清晰度。
- 相同本机 Intel UHD 730、1440×900、12 秒全程往返测试：平均动画回调频率约 37 → 53 fps；超过 50.5ms 的长帧 16 → 1–2 次。手机视口 DPR3 仿真约 60 fps，但不代表真实低端手机。
- 完整测量口径、重复结果和检查脚本见工作区 `.superdesign/review/performance-review.md`。

## 已验证

- Vite 正式构建成功。
- 本地 Edge/Chromium 桌面 1440×900、手机仿真 390×844 的六个画面实际截图。
- 额外检查 320×667、428×926、1024×768 下六个状态，正文与底部提示无重叠、无横向溢出。
- 主/移动 GLB、嵌入纹理及本地 Draco 解码正常，六个分组名称可识别。
- 先往后、再快速跨多个章节、最后回到起点：节点位置与相机坐标与初始值完全一致。
- 任意中间进度正反往返相同。最后一幕拉远阶段所有节点归位，随后展开五个分组、台基固定；反向滚动再次精确合拢，场景始终只有一个原始模型。
- 直接打开 `#interior` 镜头链接正确；移动与桌面往返改尺寸后同一组正文正确重排。
- 手机仿真中在模型区域实际执行触摸上划，页面可正常向下滚动。
- 导航跳转、自由观察入口、放大与复位、页面末尾可达。
- 移动端模型区域与文字区域不相交，无横向溢出。
- 减少动态模式使用静态完整模型，不自动拆解或移动镜头。
- 模拟 GLB 网络失败后出现重试按钮，恢复请求后加载成功。

## 限制

- 当前模型实际保留六个整组节点，不支持逐片瓦或逐件斗栱拆解；檐下段仅轻抬屋顶整组，末段按现有节点展开，不表示真实建筑的拆卸顺序。
- v02 仍是较精细的真实三维资源，初次加载约 11.5–12.6 MB，性能较弱设备会自适应降低画布清晰度；尚未做真实低端手机性能验收，也不保证所有设备稳定 60 fps。
- 本轮移动衍生版从完整 master 制作，保留彩绘天花与全部分组；细小曲面的几何精度低于桌面。旧 mobile v01 仅存档，不再作为当前首页默认资源。
- 本轮是可体验的滚动原型与对应六帧设计，等待用户确认镜头节奏与构图，不是已获批准的最终设计。
- 没有重新生成模型，没有更改 Blender 原工程，没有向外网发布。

## 文件

- `src/scroll-story.js`：滚动时间线、真实模型加载、节点还原与交互。
- `src/sen-scroll.css`：全屏近景、右侧时间线与移动重排基础；`src/porcelain-theme.css` 保留导览基础；`src/palette-theme.css` 使用用户色卡，字号沿用原有调整。
- `src/landscape-background.js` / `src/landscape-background.css`：横竖视频、静态后备、暂停和失败重试；`public/art/ARTWORK.md` 保存素材来源和早期画作提示词。
- `src/temple-rendering.js`：依据实景的材质、环境反射和缓存光影；`RENDERING-NOTES.md` 为来源和渲染说明。
- `src/components/AeroShards.jsx` / `src/ambient-background.jsx`：历史几何背景源码保留，当前主页面不载入。
- 工作区 `asset-index.json`：现有模型、设计六帧与参考素材索引。
- 工作区 `.superdesign/review/scroll-screens/`：从真实滚动页面导出的六组桌面/手机关键帧。
